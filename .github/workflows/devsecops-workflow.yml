name: Employeefrontend DevSecOps workflow

on: 
  push: 
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step 1 
        run: echo "Build Step 1"
      
      - name: Build Step 2 
        run: echo "Build Step 2"
    
  test-job:
    runs-on: ubuntu-latest
    needs: build-job
    steps: 
      - name: Test Step 1 
        run:  echo "Test job 1"
      - name: Test Step 2 
        run:  echo "Test job 2"
  provision-job:
     runs-on: ubuntu-latest
     needs: test-job
     steps: 
       - name: Provision Step 1 
         run:  echo "Provision job 1"
       - name: Provision Step 2 
         run:  echo "Provision job 2"
  deploy-job:
     runs-on: ubuntu-latest
     needs: provision-job
     steps: 
        - name: Deploy Step 1 
          run:  echo "deploy job 1"
        - name: Deploy Step 2 
          run:  echo "deploy job 2"
  monitor-job:
    runs-on: ubuntu-latest
    needs: deploy-job
    steps: 
       - name: Monitor Step 1 
         run:  echo "monitor job 1"
       - name: Monitor Step 2 
         run:  echo "monitor job 2"